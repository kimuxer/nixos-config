# NixOS Flake 常用命令速查手册

这篇手册整理了基于 **Flake** 架构的 NixOS 常用运维、清理、临时试用和排错命令。

---

## 一、 系统构建与更新 (Rebuild & Update)

每次修改完 "/etc/nixos" 下的配置文件后，需要执行构建命令才能使配置生效。

### 1. 应用配置（立即生效）
构建并立刻切换到新配置（以桌面主机为例）：

`sudo nixos-rebuild switch --flake .#nixdevbox`

或应用软路由配置：

`sudo nixos-rebuild switch --flake .#router --target-host root@192.168.10.1`

### 2. 测试配置（临时生效）
测试新配置（以桌面主机为例）：

`sudo nixos-rebuild test --flake .#nixdevbox`

### 3. 仅写入引导（重启后生效）
将新配置写入引导菜单（以桌面主机为例）：

`sudo nixos-rebuild boot --flake .#nixdevbox`

### 4. 升级所有软件源 (Inputs)
当你想要升级系统中的软件到最新版时，需要更新 flake.lock 文件：

`nix flake update`

---

## 二、 垃圾回收与存储优化 (Garbage Collect)

NixOS 默认会保留你每一次 switch 的系统版本（Generations），时间久了会占用大量磁盘空间。

### 1. 查看当前系统的历史版本

`nix-env --list-generations --profile /nix/var/nix/profiles/system`

### 2. 安全清理旧版本
删除所有比当前版本旧的历史版本（释放磁盘空间）：

`sudo nix-collect-garbage -d`

> **警告：** 清理后，你在开机 Limine 引导菜单中将无法再回滚到之前的历史版本。建议系统运行稳定后再执行。

### 3. 物理优化存储（硬链接去重）
扫描 Nix Store 并将相同的文件合并为物理硬链接。在系统运行一段时间后，该命令通常能瞬间省出数 GB 空间：

`sudo nix-store --optimise`

---

## 三、 临时试用软件 (Nix Shell)

NixOS 的灵魂特性：永远不需要为了临时用一个软件，而把它写进系统配置文件里。

### 1. 临时进入一个包含特定软件的 Shell
临时使用某个或多个工具（如 nmap, screenkey），退出该 Shell 后，这些工具会从系统中完全消失：

`nix shell nixpkgs#screenkey nixpkgs#nmap`

### 2. 临时运行一个软件（不进入新 Shell）
直接拉取并运行某命令，运行完即销毁：

`nix run nixpkgs#cowsay -- 'Hello NixOS!'`

---

## 四、 开发环境管理 (DevShell)

当你开始学习 Rust 或 C# 时，这是你在项目中最常使用的命令，能够实现项目依赖的完美隔离。

### 1. 一键进入项目专属开发环境
在包含 flake.nix（如 bread 的源码目录）或 shell.nix 的项目目录下执行：

`nix develop`

> **作用：** 它会立刻根据项目的 Flake 声明，动态加载对应的编译器版本（如特定 Rust 版本）、依赖库和环境变量，绝不污染系统全局环境。

---

## 五、 实用排错命令 (Diagnostics)

### 1. 查看服务运行状态与日志
如果 dae、kanata 或登录器 greetd 无法启动，使用 systemd 工具链查看具体报错：

`systemctl status dae`
`journalctl -f -u dae`
`journalctl -u greetd`

### 2. 检查 Flake 语法
在写完 flake.nix 之后、执行构建之前，可以用这个命令快速扫描是否有明显的语法或引用错误：

`nix flake check`


jj 初始化

jj git init --colocate

jj bookmark track main --remote=origin

jj config set --user user.name "Kim"

jj config set --user user.email "4003419+kimuxer@users.noreply.github.com"

jj metaedit --update-author

jj config set --user ui.default-command log

zeditor 在windows下显示文件编码和换行符
ctrl + , 打开setting
{
  "status_bar": {
    "line_endings_button": true,
    "active_encoding_button": "enabled",
  },
  "line_ending": "prefer_lf",
}
